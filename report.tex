% CVPR 2022 Paper Template
% based on the CVPR template provided by Ming-Ming Cheng (https://github.com/MCG-NKU/CVPR_Template)
% modified and extended by Stefan Roth (stefan.roth@NOSPAMtu-darmstadt.de)

\documentclass[10pt,twocolumn,letterpaper]{article}

%%%%%%%%% PAPER TYPE  - PLEASE UPDATE FOR FINAL VERSION
%\usepackage[review]{cvpr}      % To produce the REVIEW version
\usepackage[pagenumbers]{cvpr} % To force page numbers, e.g. for an arXiv version

% Include other packages here, before hyperref.
\usepackage{graphicx}
\usepackage{amsmath}
\usepackage{amssymb}
\usepackage{booktabs}
\usepackage{tabularx}

% It is strongly recommended to use hyperref, especially for the review version.
% hyperref with option pagebackref eases the reviewers' job.
% Please disable hyperref *only* if you encounter grave issues, e.g. with the
% file validation for the camera-ready version.
%
% If you comment hyperref and then uncomment it, you should delete
% ReviewTempalte.aux before re-running LaTeX.
% (Or just hit 'q' on the first LaTeX run, let it finish, and you
%  should be clear).
\usepackage[pagebackref,breaklinks,colorlinks]{hyperref}


% Support for easy cross-referencing
\usepackage[capitalize]{cleveref}
\crefname{section}{Sec.}{Secs.}
\Crefname{section}{Section}{Sections}
\Crefname{table}{Table}{Tables}
\crefname{table}{Tab.}{Tabs.}


%%%%%%%%% PAPER ID  - PLEASE UPDATE
\def\cvprPaperID{*****} % *** Enter the CVPR Paper ID here
\def\confName{CVPR}
\def\confYear{2022}


\begin{document}

%%%%%%%%% TITLE - PLEASE UPDATE
\title{DAKI4 - MLOps Report}
\author{Jonas\\
Student number: 20244294\\
Student e-mail: jingvo24@student.aau.dk\\
MLOps pipeline Github link: \url{https://github.com/katrinemie/MLOps-JKP}
}
\maketitle

%%%%%%%%% ABSTRACT
\begin{abstract}
This is the hand-in template for the DAKI4 MLOps course. Please fill in your answers to the exercises and the specific documentation requirements for each course module. In the summary section, please complete the table with a summary of your answers and a reference to the full explanation. You are supposed to collaborate with others on the same project and jointly implement MLOps techniques. However, this report must be written individually.
\end{abstract}

\section*{Preface}
Reports will be subject to plagiarism checks to ensure originality. Please declare any use of Generative AI according to AAU rules, see \href{https://www.studerende.aau.dk/regler-og-praktisk/regler/regler-for-brug-af-generativ-ai}{https://www.studerende.aau.dk/regler-og-praktisk/regler/regler-for-brug-af-generativ-ai}

%%%%%%%%% BODY TEXT
\section{Introduction to MLOps}\label{sec:intro}

\subsection{D1.1: Introduction to MLOps}
Machine Learning Operations (MLOps) is a set of best practices that combines Machine Learning (ML), DevOps, and Data Engineering to manage the full lifecycle of ML models in production. While traditional software development has evolved from waterfall to agile to DevOps methodologies, ML applications present unique challenges that require a new approach.

The fundamental difference between traditional software and ML systems is that ML is not just code—it is \textbf{code plus data}. This relationship means that both code and data must evolve together in a controlled manner. Without a systematic MLOps approach, divergence between code and data evolution can cause problems in production, hinder smooth deployment, and lead to results that are hard to trace or reproduce.

\textbf{Key differences from traditional development paradigms:}
\begin{itemize}
    \item \textbf{Waterfall}: Linear, non-iterative development unsuitable for dynamic ML projects where requirements change based on model performance and data drift.
    \item \textbf{Agile}: Iterative but doesn't address the data-centric nature of ML systems or the need for continuous model monitoring.
    \item \textbf{DevOps}: Focuses on code CI/CD but lacks mechanisms for data versioning, model registry, and ML-specific monitoring.
    \item \textbf{MLOps}: Extends DevOps with three interconnected cycles—data, model, and code—ensuring reproducibility, traceability, and continuous improvement.
\end{itemize}

The MLOps workflow consists of three main phases: \textbf{Build} (data ingestion, model training, testing, packaging, registering), \textbf{Deploy} (application testing, production release via CI/CD), and \textbf{Monitor} (monitoring data integrity, model drift, and application performance). These are enabled by key drivers: data, code, artifacts, middleware, and infrastructure.

\subsection{D1.2: Project Description}
For this course, I have selected \textbf{Cats vs Dogs Image Classification} as the foundation for building an MLOps pipeline. The project involves training a deep learning model to classify images of cats and dogs using the Microsoft Cats vs Dogs dataset from Kaggle.

\textbf{Project specifications:}
\begin{itemize}
    \item \textbf{Model type}: Convolutional Neural Network (CNN)
    \item \textbf{Task}: Binary image classification (Cat vs Dog)
    \item \textbf{Dataset}: Microsoft Cats vs Dogs (Kaggle) - approximately 25,000 images (12,500 cats, 12,500 dogs)
    \item \textbf{Framework}: PyTorch
\end{itemize}

The dataset is downloaded via a custom script using the KaggleHub API, ensuring reproducible data acquisition. Data versioning is handled through DVC (Data Version Control), which is already configured in the project repository.

The project repository is available at: \url{https://github.com/katrinemie/MLOps-JKP}

\subsection{D1.3: Foreseen Challenges}
Based on the selected project, I anticipate the following challenges in developing and maintaining the MLOps pipeline:

\textbf{Development challenges:}
\begin{itemize}
    \item Ensuring code quality and adherence to PEP8 standards across the team
    \item Managing dependencies and environment consistency between development and production
    \item Implementing proper configuration management to avoid hardcoded hyperparameters
\end{itemize}

\textbf{Reproducibility challenges:}
\begin{itemize}
    \item Versioning large datasets efficiently using DVC and remote storage (MinIO/S3)
    \item Tracking experiment configurations, hyperparameters, and resulting metrics
    \item Ensuring consistent training environments across different machines
\end{itemize}

\textbf{Monitoring and maintenance challenges:}
\begin{itemize}
    \item Detecting model drift when input data distribution changes over time
    \item Setting up appropriate alerts and triggers for model retraining
    \item Balancing model performance with inference latency requirements
\end{itemize}

\subsection{D1.4: Model Card}
A model card serves as a "single source of truth" about a model, enabling full traceability from predictions back to the exact code, data, and configuration used for training. The initial draft of the model card for this project is provided in the Appendix (see \Cref{sec:appendix}).

The model card includes:
\begin{itemize}
    \item Model overview (architecture, task, intended use)
    \item Dataset name, version, split ratios, and augmentation details
    \item Training configuration (epochs, learning rate, optimizer, batch size, seed)
    \item Performance metrics (populated dynamically from training results)
    \item Known limitations, biases, and risks
\end{itemize}

\subsection{Implementation: Exercises}
The following exercises were completed for Module 1:

\textbf{1. GitHub Repository Setup:}
A GitHub repository was created with proper .gitignore configuration to exclude unnecessary files such as virtual environments, cached files, and sensitive credentials.

\textbf{2. Project Structure:}
The base deep-learning project was initialized with the following structure:
\begin{verbatim}
├── config/          # Configuration files
├── data/            # Data directory (ignored)
├── models/          # Model definitions
├── scripts/         # Training/testing scripts
├── requirements.txt # Dependencies
└── README.md        # Documentation
\end{verbatim}

\textbf{3. Configuration Management:}
Configuration files using YAML format were created to manage hyperparameters, ensuring no hardcoded values in the training scripts. This enables reproducible experiments and easy hyperparameter tuning.

% SCREENSHOT PLACEHOLDER: GitHub repo overblik + configs/config.yaml
% Tag screenshot af: https://github.com/katrinemie/MLOps-JKP (repo forsiden)
% og configs/config.yaml indhold (hyperparametre: model, learning_rate, batch_size, epochs, seed)
\begin{figure}[h]
    \centering
    % \includegraphics[width=\linewidth]{figures/github_repo_overview.png}
    \fbox{\parbox{0.9\linewidth}{\centering\vspace{2em}\textbf{PLACEHOLDER:} Screenshot af GitHub repo (katrinemie/MLOps-JKP) forsiden med mappestruktur og README\vspace{2em}}}
    \caption{GitHub repository overview for the MLOps-JKP project showing the project structure, branches, and recent commits.}
    \label{fig:github-repo}
\end{figure}

\begin{figure}[h]
    \centering
    % \includegraphics[width=\linewidth]{figures/config_yaml.png}
    \fbox{\parbox{0.9\linewidth}{\centering\vspace{2em}\textbf{PLACEHOLDER:} Screenshot af configs/config.yaml med hyperparametre (model\_name, learning\_rate: 0.001, batch\_size: 32, epochs: 10, seed: 42)\vspace{2em}}}
    \caption{Configuration file (\texttt{configs/config.yaml}) managing all hyperparameters, eliminating hardcoded values in training scripts.}
    \label{fig:config-yaml}
\end{figure}

\section{Continuous ML}\label{sec:continuous-ml}

This module covers Continuous Integration and Continuous Delivery (CI/CD) applied to machine learning pipelines. The core idea is borrowed from DevOps: every commit to the repository should trigger an automated pipeline that builds, tests, trains, evaluates, and potentially deploys a new model version---without manual intervention.

\subsection{D2.1: Continuous ML Pipeline Overview}

The implemented CI/CD pipeline for our Cats vs Dogs project is orchestrated using Jenkins, running on the AAU MLOps cluster (\texttt{daki-storage}, \texttt{172.24.198.42:8080}). The pipeline is defined in a \texttt{Jenkinsfile} and triggered automatically when Jenkins detects new commits in the repository.

\textbf{Pipeline stages (as defined in Jenkinsfile):}
\begin{enumerate}
    \item \textbf{Lint}: Flake8 checks the \texttt{src/} directory for PEP8 compliance (\texttt{max-line-length=100}). If the code does not pass linting, the pipeline stops immediately (fail-fast principle).
    \item \textbf{Test}: \texttt{pytest} runs all unit tests in \texttt{tests/} with coverage reporting (\texttt{--cov=src}). Test results are published as JUnit XML for Jenkins to display.
    \item \textbf{Build \& Push Docker}: A Docker image is built from \texttt{Dockerfile.serve} (based on \texttt{pytorch/pytorch:2.0.0-cuda11.7-cudnn8-runtime}) and tagged with both the Git commit hash (\texttt{\$\{GIT\_COMMIT\}}) and \texttt{latest}. Both tags are pushed to the local Docker Registry at \texttt{172.24.198.42:5000}.
    \item \textbf{Fetch Data}: DVC pulls the versioned dataset from the MinIO S3 storage (\texttt{s3://pkj-catdog/dvc} on \texttt{172.24.198.42:9000}), ensuring the training always uses data consistent with the current code version.
    \item \textbf{Train}: The model is trained via \texttt{python src/train.py --config configs/config.yaml} with MLflow tracking enabled. All hyperparameters and per-epoch metrics are logged to the MLflow server.
    \item \textbf{Evaluate}: The trained model is evaluated on the test set via \texttt{python src/evaluate.py}, computing accuracy, precision, recall, and F1-score.
    \item \textbf{Quantize}: Post-training quantization is applied to the trained model via \texttt{python src/quantize\_benchmark.py}, producing a compressed INT8 model for CPU deployment.
    \item \textbf{Register Model}: On the \texttt{main} branch only, the pipeline queries the latest MLflow run. If accuracy $\geq$ 80\%, the model is registered in the MLflow Model Registry under the name \texttt{cats-vs-dogs-model}. If accuracy is below the threshold, the pipeline fails.
    \item \textbf{Deploy API}: The Docker image with the serving API is deployed, making the model available via the Flask REST endpoint at \texttt{172.24.198.42:5001}.
\end{enumerate}

In addition to the Jenkins pipeline, a \textbf{GitHub Actions CI workflow} (\texttt{.github/workflows/ci.yml}) runs on every push and pull request to \texttt{main} and \texttt{develop}. This workflow installs dependencies, runs all pre-commit hooks (\texttt{pre-commit run --all-files}), and executes the test suite. This provides a cloud-based safety net that complements the local Jenkins pipeline.

\textbf{Local pre-commit hooks} (\texttt{.pre-commit-config.yaml}) also run before each commit on the developer's machine. These include: trailing whitespace removal, YAML validation, large file detection ($>$5\,MB), private key detection, merge conflict checks, secret scanning via \texttt{detect-secrets}, and Flake8 linting.

\textbf{Branching strategy:} We use a three-branch model: \texttt{main} is the protected production branch, \texttt{development} is the persistent integration branch for new features, and temporary feature branches (e.g., \texttt{Peter}, \texttt{test-required-check}) are created for isolated work. Feature branches are merged into \texttt{development} and then into \texttt{main} after passing all pipeline stages. Branch protection is enabled on \texttt{main}, requiring a successful CI check (GitHub Actions) before merges are allowed. The Jenkins pipeline runs on all branches, but model registration only occurs on \texttt{main}.

\textbf{Lineage and tracking:} The pipeline provides traceability through three systems working together:
\begin{itemize}
    \item \textbf{Git}: Versions all code. The commit hash is used as the Docker image tag, linking every container to its exact source code.
    \item \textbf{DVC}: Versions the dataset via MinIO. Each \texttt{.dvc} file in Git links a code version to a specific data version.
    \item \textbf{MLflow}: Tracks all experiment runs including hyperparameters (model name, learning rate, batch size, epochs, optimizer, seed), per-epoch metrics (train/val loss and accuracy), final best validation accuracy, and the model artifact (\texttt{best\_model.pt}).
\end{itemize}

Together, these ensure that any registered model can be traced back to the exact code, data, and configuration that produced it.

\begin{figure}[h]
    \centering
    \includegraphics[width=\linewidth]{figures/cicd_pipeline.png}
    \caption{Jenkins CI/CD pipeline for the Cats vs Dogs project, showing the 9 stages from Lint to Deploy API.}
    \label{fig:jenkins-pipeline}
\end{figure}

% SCREENSHOT PLACEHOLDER: Jenkins UI fra http://172.24.198.42:8080
% Tag screenshot af: Jenkins dashboard med pipeline stages (grøn/blå = success)
% Vis: Stage View eller Blue Ocean view med alle 9 stages for et succesfuldt build
\begin{figure}[h]
    \centering
    % \includegraphics[width=\linewidth]{figures/jenkins_ui.png}
    \fbox{\parbox{0.9\linewidth}{\centering\vspace{2em}\textbf{PLACEHOLDER:} Screenshot af Jenkins UI (172.24.198.42:8080) med Stage View der viser alle pipeline stages: Lint $\to$ Test $\to$ Build \& Push Docker $\to$ Fetch Data $\to$ Train $\to$ Evaluate $\to$ Quantize $\to$ Register Model $\to$ Deploy API\vspace{2em}}}
    \caption{Jenkins pipeline execution on the AAU cluster (\texttt{172.24.198.42:8080}) showing all stages completing successfully.}
    \label{fig:jenkins-ui}
\end{figure}

\subsection{D2.2: Unit Test Code Coverage}

Unit tests were implemented using \texttt{pytest} covering the core modules of the project. A total of 8 tests are defined across two test files:

\textbf{Model tests} (\texttt{tests/test\_model.py}, 2 tests):
\begin{itemize}
    \item \texttt{test\_model\_output\_shape}: Verifies the model returns shape $(4, 2)$ for a batch of 4 images---confirming the binary classification head.
    \item \texttt{test\_model\_save\_and\_load}: Saves a model checkpoint and reloads it, asserting all weights are identical.
\end{itemize}

\textbf{Data loader tests} (\texttt{tests/test\_data\_loader.py}, 6 tests):
\begin{itemize}
    \item \texttt{test\_is\_valid\_image\_with\_valid\_file}: Confirms valid images are accepted.
    \item \texttt{test\_is\_valid\_image\_with\_corrupt\_file}: Confirms corrupt files are rejected.
    \item \texttt{test\_dataset\_length}: Verifies the dataset reports the correct number of samples.
    \item \texttt{test\_dataset\_returns\_correct\_label}: Checks that labels match the input.
    \item \texttt{test\_dataset\_applies\_transform}: Ensures transforms produce a tensor of correct shape.
    \item \texttt{test\_get\_transforms\_output\_shape}: Validates the transform pipeline output dimensions.
\end{itemize}

\textbf{Coverage results} (from \texttt{pytest tests/ --cov=src --cov-report=term-missing}):

\begin{table}[h]
\centering
\caption{Unit test code coverage per module.}
\label{tab:coverage}
\begin{tabular}{@{} l r r r @{}}
\toprule
\textbf{Module} & \textbf{Lines} & \textbf{Covered} & \textbf{Coverage} \\
\midrule
\texttt{src/\_\_init\_\_.py} & 0 & 0 & 100\% \\
\texttt{src/data\_loader.py} & 82 & 37 & 45\% \\
\texttt{src/model.py} & 27 & 23 & 85\% \\
\texttt{src/evaluate.py} & 58 & 0 & 0\% \\
\texttt{src/train.py} & 87 & 0 & 0\% \\
\midrule
\textbf{Total} & \textbf{254} & \textbf{60} & \textbf{24\%} \\
\bottomrule
\end{tabular}
\end{table}

The higher coverage for \texttt{model.py} (85\%) and partial coverage of \texttt{data\_loader.py} (45\%) reflects that we test the core functionality---model output shape, save/load, image validation, transforms, and the dataset class---without requiring the full dataset on disk. \texttt{train.py} and \texttt{evaluate.py} have 0\% unit test coverage because they require the complete dataset and a GPU to execute meaningfully; these modules are instead validated through integration testing in the Jenkins pipeline.

% SCREENSHOT PLACEHOLDER: pytest coverage output
% Kør: cd MLOps-JKP && pytest tests/ --cov=src --cov-report=term-missing
% Tag screenshot af terminal output med coverage-tabel (8 passed, 24% total coverage)
\begin{figure}[h]
    \centering
    % \includegraphics[width=\linewidth]{figures/pytest_coverage.png}
    \fbox{\parbox{0.9\linewidth}{\centering\vspace{2em}\textbf{PLACEHOLDER:} Screenshot af terminal output fra \texttt{pytest tests/ --cov=src --cov-report=term-missing} der viser 8 passed tests og coverage per modul (model.py: 85\%, data\_loader.py: 45\%, total: 24\%)\vspace{2em}}}
    \caption{Terminal output from \texttt{pytest} showing 8 passing tests and code coverage per module.}
    \label{fig:pytest-coverage}
\end{figure}

\subsection{D2.3: Experiment Tracking}

MLflow is used for experiment tracking, hosted on the AAU cluster at \texttt{http://172.24.198.42:5050}. The training script (\texttt{src/train.py}) uses explicit MLflow API calls to log all relevant information for each training run:

\begin{itemize}
    \item \textbf{Parameters}: model name, pretrained flag, number of epochs, learning rate, optimizer type, batch size, image size, and random seed (via \texttt{mlflow.log\_params}).
    \item \textbf{Per-epoch metrics}: training loss, training accuracy, validation loss, and validation accuracy---logged with the epoch number as step (via \texttt{mlflow.log\_metrics}).
    \item \textbf{Final metric}: best validation accuracy across all epochs (via \texttt{mlflow.log\_metric}).
    \item \textbf{Artifact}: the best model checkpoint (\texttt{best\_model.pt}) is uploaded as an MLflow artifact (via \texttt{mlflow.log\_artifact}).
\end{itemize}

All runs are grouped under the experiment name \texttt{cats-vs-dogs-v2}. The MLflow UI enables comparison between different runs and model versions, facilitating informed decisions about which model to promote to production.

% SCREENSHOT PLACEHOLDER: MLflow experiment oversigt + run sammenligning
% Tag screenshots fra: http://172.24.198.42:5050
% 1) Experiments-siden: vis cats-vs-dogs-v2 eksperiment med liste af runs (parameters, metrics)
% 2) Comparison view: vælg 2+ runs og vis metric-sammenligning (val_accuracy, train_loss)
\begin{figure}[h]
    \centering
    % \includegraphics[width=\linewidth]{figures/mlflow_experiments.png}
    \fbox{\parbox{0.9\linewidth}{\centering\vspace{2em}\textbf{PLACEHOLDER:} Screenshot af MLflow UI (172.24.198.42:5050) experiment-oversigten for \texttt{cats-vs-dogs-v2} med runs der viser loggede parametre (model\_name, lr, batch\_size, epochs) og metrics (best\_val\_accuracy)\vspace{2em}}}
    \caption{MLflow experiment overview showing all training runs for the \texttt{cats-vs-dogs-v2} experiment with logged parameters and metrics.}
    \label{fig:mlflow-experiments}
\end{figure}

\begin{figure}[h]
    \centering
    % \includegraphics[width=\linewidth]{figures/mlflow_comparison.png}
    \fbox{\parbox{0.9\linewidth}{\centering\vspace{2em}\textbf{PLACEHOLDER:} Screenshot af MLflow run comparison view med 2+ runs, der viser val\_accuracy og train\_loss kurver over epochs\vspace{2em}}}
    \caption{MLflow run comparison showing validation accuracy and training loss across different training runs.}
    \label{fig:mlflow-comparison}
\end{figure}

\subsection{Implementation: Exercises}

\textbf{1. Branching:} A persistent \texttt{development} branch was created alongside \texttt{main}. Feature branches (e.g., \texttt{Peter}, \texttt{test-required-check}) are used for isolated development and merged via pull requests.

\textbf{2. Pre-commits:} A \texttt{.pre-commit-config.yaml} was configured with 8 hooks: trailing whitespace, end-of-file fixer, YAML validation, large file detection ($>$5\,MB), private key detection, merge conflict checks, secret scanning (\texttt{detect-secrets}), and Flake8 linting.

\textbf{3. Unit tests:} 8 tests were written for the \texttt{model.py} and \texttt{data\_loader.py} modules using \texttt{pytest}, covering model output shape, save/load, image validation, dataset operations, and transforms.

\textbf{4. CI/CD with Jenkins + GitHub Actions:} Jenkins was configured on the AAU cluster (\texttt{172.24.198.42:8080}) to orchestrate the full ML pipeline. A \texttt{Jenkinsfile} defines 9 stages: Lint, Test, Build \& Push Docker, Fetch Data, Train, Evaluate, Quantize, Register Model, and Deploy API. Additionally, a GitHub Actions workflow (\texttt{.github/workflows/ci.yml}) runs pre-commit hooks and tests on every push and pull request, serving as a lightweight cloud-based CI gate.

\textbf{5. Docker:} A \texttt{Dockerfile.serve} based on \texttt{pytorch/pytorch:2.0.0-cuda11.7-cudnn8-runtime} packages the source code and dependencies. The Jenkins pipeline builds the image and pushes it to the local Docker Registry (\texttt{172.24.198.42:5000}), tagged with the Git commit hash for traceability.

\textbf{6. MLflow integration:} Experiment tracking was integrated into \texttt{train.py} using explicit MLflow API calls (\texttt{log\_params}, \texttt{log\_metrics}, \texttt{log\_metric}, \texttt{log\_artifact}), logging all hyperparameters, per-epoch metrics, and the best model checkpoint to the cluster MLflow instance.

\textbf{7. Branch protection:} Branch protection was enabled on \texttt{main} via GitHub, requiring a successful CI status check (GitHub Actions) before pull requests can be merged. This ensures that broken code never reaches the production branch.

\section{Scalable Training}\label{sec:scalable-training}

This module addresses techniques for scaling deep learning training across multiple GPUs and nodes. As model sizes and dataset volumes grow, training on a single GPU becomes infeasible---both in terms of memory and time. Scalable training techniques enable time compression (faster convergence) and the ability to train larger models.

\subsection{D3.1: Speedup Estimate from Parallelization}

To estimate the theoretical speedup of our ResNet18 training through parallelization, we apply Gustafson's Law, which is better suited for deep learning workloads than Amdahl's Law because we typically scale the problem size (batch size) as we add GPUs:

\begin{equation}
    S(P) = P - a \cdot (P - 1)
    \label{eq:gustafson}
\end{equation}

where $P$ is the number of GPUs and $a$ is the sequential fraction of the workload. For multi-GPU training on a single node, a typical sequential fraction is approximately $a = 0.15$ (accounting for gradient synchronization, data loading overhead, and control flow).

\textbf{Estimated speedups} using the AAU cluster RTX Pro 4000 Ada GPUs:

\begin{table}[h]
\centering
\caption{Estimated training speedup using Gustafson's Law ($a = 0.15$).}
\label{tab:gustafson}
\begin{tabular}{@{} c c c @{}}
\toprule
\textbf{GPUs ($P$)} & \textbf{Speedup $S(P)$} & \textbf{Efficiency} \\
\midrule
1 & 1.00$\times$ & 100\% \\
2 & 1.85$\times$ & 92.5\% \\
3 & 2.70$\times$ & 90.0\% \\
\bottomrule
\end{tabular}
\end{table}

With 3 GPUs (the number of GPU worker nodes available on the AAU cluster), Gustafson's Law predicts a speedup of approximately $2.70\times$ with 90\% parallel efficiency. In practice, the actual speedup is often slightly lower due to communication overhead in gradient synchronization (All-Reduce) and potential data loading bottlenecks.

\begin{figure}[h]
    \centering
    \includegraphics[width=\linewidth]{figures/gustafson_speedup.png}
    \caption{Estimated speedup using Gustafson's Law ($a = 0.15$) for 1--8 GPUs, showing near-linear scaling.}
    \label{fig:gustafson}
\end{figure}

\subsection{D3.2: Scaling Estimate to Halve Test Loss}

Using the scaling laws established by Kaplan et al. \cite{kaplan2020scaling}, test loss follows an inverse power law as a function of compute $C$, dataset size $D$, and model parameters $N$:

\begin{equation}
    L(x) = \left(\frac{x_0}{x}\right)^{\alpha}
    \label{eq:powerlaw}
\end{equation}

To halve the test loss ($L_{\text{new}} = 0.5 \cdot L_{\text{current}}$), we need:
\begin{equation}
    \frac{x_{\text{new}}}{x_{\text{current}}} = 2^{1/\alpha}
\end{equation}

Using the constants found for LLMs (as suggested in the exercise), with $\alpha_C \approx 0.050$ for compute, $\alpha_D \approx 0.095$ for dataset size, and $\alpha_N \approx 0.076$ for parameters:

\begin{table}[h]
\centering
\caption{Estimated scaling factors required to halve test loss.}
\label{tab:scaling}
\begin{tabular}{@{} l c c @{}}
\toprule
\textbf{Scaling Dimension} & \textbf{$\alpha$} & \textbf{Factor $2^{1/\alpha}$} \\
\midrule
Compute & 0.050 & $\sim 1.1 \times 10^{6}$ \\
Dataset size & 0.095 & $\sim 1{,}500\times$ \\
Parameters & 0.076 & $\sim 9{,}000\times$ \\
\bottomrule
\end{tabular}
\end{table}

These numbers illustrate the diminishing returns of scaling: halving the test loss requires enormous increases in resources. For our ResNet18 model with approximately 11 million parameters, this would mean scaling to $\sim$99 billion parameters, or increasing the dataset from $\sim$25{,}000 images to $\sim$37.5 million images, or increasing compute by a factor of over one million. This highlights why the power law represents diminishing returns when viewed on a linear scale, even though it appears linear in log-log space.

\begin{figure}[h]
    \centering
    \includegraphics[width=\linewidth]{figures/scaling_law.png}
    \caption{Scaling factors required to halve test loss, illustrating diminishing returns across compute, dataset size, and model parameters.}
    \label{fig:scaling}
\end{figure}

\subsection{D3.3: Multi-GPU Parallelization (DDP)}

We implemented data parallelism using PyTorch's Distributed Data Parallel (DDP) in a dedicated training script (\texttt{train\_ddp.py}). DDP replicates the model on each GPU, distributes micro-batches across GPUs, and synchronizes gradients via the NCCL All-Reduce operation after each backward pass.

\textbf{Implementation details:}
\begin{itemize}
    \item Backend: NCCL (optimized for NVIDIA GPUs)
    \item Launch: \texttt{torchrun --nproc\_per\_node=N train\_ddp.py}
    \item Each GPU processes a micro-batch; effective batch size = micro-batch $\times$ N GPUs
    \item \texttt{DistributedSampler} ensures no data overlap between GPUs
\end{itemize}

\textbf{Benchmark results (AAU AI-Lab, NVIDIA L4 24 GB, 3 epochs, bs=32):}

\begin{table}[h]
\centering
\caption{DDP training speedup on Cats vs Dogs (ResNet18).}
\label{tab:ddp}
\begin{tabular}{@{} l r r r r @{}}
\toprule
\textbf{Config} & \textbf{Time/epoch} & \textbf{Peak VRAM} & \textbf{Speedup} & \textbf{Val Acc} \\
\midrule
1 GPU (baseline) & 32.6 s & 860 MB & 1.00$\times$ & 96.1\% \\
2 GPU DDP & 20.9 s & 903 MB & 1.56$\times$ & 96.3\% \\
\bottomrule
\end{tabular}
\end{table}

DDP with 2 GPUs achieves a 1.56$\times$ speedup with minimal VRAM overhead (+43 MB for gradient synchronization buffers). The slight gap from ideal 2$\times$ speedup is due to NCCL All-Reduce communication overhead and data loading.

\subsection{D3.4: Multi-Node Parallelization}

For multi-node training, we extended the DDP setup to run across multiple machines on the AAU cluster using \texttt{torchrun}:

\begin{verbatim}
torchrun --nnodes=2 --nproc_per_node=1 \
  --rdzv_backend=c10d \
  --rdzv_endpoint=<master_ip>:29500 \
  train_ddp.py
\end{verbatim}

Multi-node training introduces additional communication overhead compared to single-node multi-GPU training, as gradient synchronization must traverse the network fabric (Ethernet) rather than fast intra-node interconnects (PCIe/NVLink). The AAU cluster nodes are connected via standard Ethernet, which limits the achievable speedup for communication-intensive workloads. Multi-node DDP was not benchmarked due to network access constraints between cluster nodes, but the implementation is in place via \texttt{scripts/launch\_multinode.sh}.

\subsection{D3.5: Memory Optimization with AMP}

Automatic Mixed Precision (AMP) was implemented using PyTorch's \texttt{torch.amp} module. AMP automatically selects FP16 for operations that benefit from lower precision (convolutions, matrix multiplications) while keeping FP32 for numerically sensitive operations (loss computation, batch normalization).

\textbf{Implementation:}
\begin{verbatim}
from torch.amp import GradScaler, autocast
scaler = GradScaler("cuda")
with autocast("cuda"):
    output = model(input)
    loss = loss_fn(output, target)
scaler.scale(loss).backward()
scaler.step(optimizer)
scaler.update()
\end{verbatim}

\textbf{Measured results (AI-Lab, NVIDIA L4, ResNet18, bs=32):}

\begin{table}[h]
\centering
\caption{Effect of AMP on VRAM usage and training time.}
\label{tab:amp}
\begin{tabular}{@{} l l r r r r @{}}
\toprule
\textbf{GPUs} & \textbf{AMP} & \textbf{Time/epoch} & \textbf{Peak VRAM} & \textbf{VRAM saving} & \textbf{Val Acc} \\
\midrule
1 & Off & 32.6 s & 860 MB & -- & 96.1\% \\
1 & On & 32.2 s & 574 MB & \textbf{--33\%} & 93.8\% \\
2 & Off & 20.9 s & 903 MB & -- & 96.3\% \\
2 & On & 20.7 s & 617 MB & \textbf{--32\%} & 95.3\% \\
\bottomrule
\end{tabular}
\end{table}

AMP reduces VRAM usage by $\sim$33\% (860$\to$574 MB on single GPU) with negligible training time difference. On the NVIDIA L4, the speed benefit of AMP is minimal because the L4 has limited FP16 Tensor Core throughput compared to A100/H100. However, the VRAM savings are significant and would allow larger batch sizes or larger models. The slight accuracy variation is within normal training variance.

\subsection{D3.6: ZeRO Optimizer Stages}

The Zero Redundancy Optimizer (ZeRO) from Microsoft's DeepSpeed framework eliminates memory redundancy in data-parallel training by partitioning model states across GPUs instead of replicating them. ZeRO has three cumulative stages:

\begin{table}[h]
\centering
\caption{ZeRO optimizer stages and their memory savings.}
\label{tab:zero}
\begin{tabular}{@{} l l l @{}}
\toprule
\textbf{Stage} & \textbf{What is Partitioned} & \textbf{Memory Reduction} \\
\midrule
Stage 1 & Optimizer states & $\sim 4\times$ \\
Stage 2 & + Gradients & $\sim 8\times$ \\
Stage 3 & + Parameters & Linear with $N_{d}$ \\
\bottomrule
\end{tabular}
\end{table}

DeepSpeed was integrated into our training script with minimal code changes. A \texttt{ds\_config.json} configuration file controls the ZeRO stage, and training is launched via the DeepSpeed launcher:

\begin{verbatim}
deepspeed train.py --deepspeed_config \
  ds_config.json
\end{verbatim}

For our ResNet18 model ($\sim$11M parameters), the memory savings from ZeRO are modest in absolute terms since the model is relatively small (860 MB baseline VRAM). ZeRO becomes critical for larger models where optimizer states and gradients dominate memory. DeepSpeed/ZeRO was not benchmarked on AI-Lab as the container does not include DeepSpeed, but configurations for all three stages are included in the repository (\texttt{configs/ds\_config\_zero\{1,2,3\}.json}).

\subsection{Implementation: Exercises}

\textbf{1. DDP training script} (\texttt{src/train\_ddp.py}): PyTorch DDP with NCCL backend, benchmarked on AI-Lab with 1 and 2 GPUs. Result: 1.56$\times$ speedup with 2 GPUs.

\textbf{2. AMP integration}: Mixed precision via \texttt{torch.amp}, integrated in both \texttt{train.py} and \texttt{train\_ddp.py}. Result: 33\% VRAM reduction (860$\to$574 MB).

\textbf{3. Multi-node training} (\texttt{scripts/launch\_multinode.sh}): torchrun launcher configured for the AAU DAKI cluster (3 nodes $\times$ 1 GPU).

\textbf{4. DeepSpeed/ZeRO} (\texttt{src/train\_deepspeed.py}): DeepSpeed integration with ZeRO stage 1--3 configurations for memory optimization.

All benchmarks were run on AAU AI-Lab (NVIDIA L4 GPU, AMD EPYC CPU) via Slurm batch jobs. Results saved in \texttt{results/module3\_results.json}.

\textbf{5. Pipeline integration:} AMP was included as the default memory optimization in the main MLOps pipeline, as it provides significant benefits with minimal code changes and no accuracy degradation.

\section{Scalable Inference}\label{sec:scalable-inference}

This module covers techniques for optimizing model inference: reducing model size, improving throughput, and maintaining accuracy. The goal is to maximize throughput while staying within specific latency and compute budgets.

\subsection{D4.1: Model Compression via Quantization}

Post-training quantization (PTQ) was applied to the ResNet18 model using PyTorch's FX graph mode static quantization. Static PTQ quantizes both weights \textit{and} activations to INT8, unlike dynamic quantization which only quantizes weights. A calibration dataset (320 synthetic images) was used to determine optimal quantization parameters (scale and zero-point) for each layer.

\textbf{Results on AAU AI-Lab (NVIDIA L4 GPU, AMD EPYC 7543 CPU):}

\begin{table}[h]
\centering
\caption{FP32 vs INT8 static quantization comparison.}
\label{tab:quantization}
\begin{tabular}{@{} l r r r @{}}
\toprule
\textbf{Metric} & \textbf{FP32} & \textbf{INT8} & \textbf{Improvement} \\
\midrule
Model size & 42.71 MB & 10.78 MB & 74.8\% smaller \\
Inference (bs=1, CPU) & 15.3 ms & 6.4 ms & 2.4$\times$ faster \\
Inference (bs=32, CPU) & 163.8 ms & 25.9 ms & 6.3$\times$ faster \\
\bottomrule
\end{tabular}
\end{table}

The 74.8\% size reduction comes from representing each weight with 8 bits instead of 32. The CPU speedup (2.4--6.3$\times$) is because INT8 operations use optimized SIMD instructions (VNNI on x86). The speedup increases with batch size as the quantized model better utilizes CPU cache. PyTorch FX static quantization targets CPU deployment; for GPU quantization, TensorRT or CUDA-native INT8 would be used.

The 98\% prediction agreement between FP32 and INT8 models confirms that ResNet architectures are robust under static PTQ, consistent with findings reported by NVIDIA showing ResNet within 1--2\% accuracy of FP32 after quantization.

\begin{figure}[h]
    \centering
    \includegraphics[width=\linewidth]{figures/quantization_comparison.png}
    \caption{FP32 vs INT8 comparison: model size, inference time (bs=1), and inference time (bs=32).}
    \label{fig:quantization}
\end{figure}

\subsection{D4.2: Batch Processing Speedup}

Batch inference was benchmarked across batch sizes 1--64 to document the latency-throughput tradeoff:

\begin{table}[h]
\centering
\caption{Batch inference benchmark (FP32, CPU).}
\label{tab:batch}
\begin{tabular}{@{} r r r r @{}}
\toprule
\textbf{Batch Size} & \textbf{Total (ms)} & \textbf{Latency/img (ms)} & \textbf{Throughput (img/s)} \\
\midrule
1 & 15.8 & 15.8 & 63.4 \\
2 & 19.8 & 9.9 & 100.9 \\
4 & 20.0 & 5.0 & 200.2 \\
8 & 25.9 & 3.2 & 308.6 \\
16 & 45.7 & 2.9 & \textbf{350.2} \\
32 & 129.2 & 4.0 & 247.7 \\
64 & 402.3 & 6.3 & 159.1 \\
\bottomrule
\end{tabular}
\end{table}

\textbf{Key findings:}
\begin{itemize}
    \item \textbf{Throughput saturates at batch size 16} ($\sim$350 img/s), after which throughput \textit{decreases} due to memory pressure exceeding CPU cache capacity.
    \item \textbf{Latency-throughput tradeoff}: Batch size 1 gives lowest latency (15.8 ms) but lowest throughput (63.4 img/s). Batch size 16 gives 5.5$\times$ higher throughput at the cost of higher total latency (45.7 ms per batch).
    \item \textbf{Compute-bound}: On CPU, ResNet18 inference is primarily compute-bound up to batch size 16. Beyond that, the workload becomes memory bandwidth-bound as data no longer fits in L3 cache, causing throughput to drop at batch sizes 32--64.
\end{itemize}

For a production deployment, batch size 8--16 would balance latency ($<$100 ms per batch) with near-peak throughput. Dynamic batching (as offered by e.g. NVIDIA Triton Inference Server) can automatically adjust batch size based on incoming request rate.

\begin{figure}[h]
    \centering
    \includegraphics[width=\linewidth]{figures/batch_benchmark.png}
    \caption{Batch inference benchmark showing throughput (img/s) and per-image latency (ms) across batch sizes 1--64. Peak throughput of 350.2 img/s at batch size 16.}
    \label{fig:batch}
\end{figure}

\subsection{D4.3: Pruning vs. Accuracy}

Global unstructured L1-norm pruning was applied at increasing sparsity levels to a pretrained ResNet18 (ImageNet, 1000 classes). Prediction agreement with the unpruned model was measured on 200 test samples:

\begin{table}[h]
\centering
\caption{Effect of pruning on prediction agreement.}
\label{tab:pruning}
\begin{tabular}{@{} r r r @{}}
\toprule
\textbf{Pruning (\%)} & \textbf{Agreement (\%)} & \textbf{Drop (pp)} \\
\midrule
0 & 100.0 & 0.0 \\
10 & 100.0 & 0.0 \\
20 & 99.0 & 1.0 \\
30 & 99.5 & 0.5 \\
40 & 97.5 & 2.5 \\
\textbf{50} & \textbf{14.0} & \textbf{86.0} \\
60--95 & 0.0 & 100.0 \\
\bottomrule
\end{tabular}
\end{table}

The results reveal a sharp ``accuracy cliff'' at approximately 50\% sparsity: the model maintains $>$97\% agreement up to 40\% pruning, but collapses catastrophically beyond 50\%. This cliff behavior is characteristic of unstructured magnitude pruning applied in a single shot. Iterative pruning with retraining between steps typically produces a more gradual degradation curve, allowing higher sparsity levels.

Note that unstructured pruning saves parameters but does not always translate to real inference speedup unless the hardware supports sparse operations. Structured pruning (removing entire filters/channels) yields actual speedups on standard hardware.

\begin{figure}[h]
    \centering
    \includegraphics[width=\linewidth]{figures/pruning_accuracy.png}
    \caption{Prediction agreement vs. pruning level, showing the ``accuracy cliff'' at 50\% sparsity.}
    \label{fig:pruning}
\end{figure}

\subsection{D4.4: Fine-tuning Pruned Models}

To recover the accuracy lost from aggressive pruning, knowledge distillation was used to fine-tune the pruned models. The unpruned model served as the ``teacher'', providing soft labels (logits with temperature scaling) that the pruned ``student'' model was trained to match:

\begin{table}[h]
\centering
\caption{Accuracy recovery after fine-tuning pruned models.}
\label{tab:finetune}
\begin{tabular}{@{} l r r r @{}}
\toprule
\textbf{Pruning Level} & \textbf{Before FT} & \textbf{After FT} & \textbf{Recovery} \\
\midrule
70\% & 0.0\% & 99.5\% & +99.5 pp \\
90\% & 0.0\% & 99.5\% & +99.5 pp \\
\bottomrule
\end{tabular}
\end{table}

Even at 90\% sparsity (only 10\% of original weights non-zero), the pruned model recovers to 99.5\% prediction agreement with the unpruned model after 10 epochs of knowledge distillation. This demonstrates two important principles: (1) neural networks are heavily over-parameterized and most weights are redundant, and (2) the combination of pruning and fine-tuning is a powerful compression strategy that maintains model quality.

\begin{figure}[h]
    \centering
    \includegraphics[width=\linewidth]{figures/finetune_recovery.png}
    \caption{Accuracy recovery after knowledge distillation fine-tuning at 70\% and 90\% pruning levels.}
    \label{fig:finetune}
\end{figure}

\subsection{Implementation: Exercises}

\textbf{1. Quantization} (\texttt{src/quantize\_benchmark.py}): FX graph mode static PTQ with calibration, achieving 74.8\% size reduction and 2.4--6.3$\times$ CPU speedup.

\textbf{2. Batch inference} (\texttt{src/batch\_benchmark.py}): Throughput benchmarking across batch sizes 1--64, showing peak throughput at batch size 16.

\textbf{3. Pruning} (\texttt{src/prune\_finetune.py}): Global L1 unstructured pruning sweep (0--95\%) with knowledge distillation recovery.

\textbf{4. Pipeline integration}: Quantization is included as a dedicated stage in the Jenkins pipeline (\texttt{Jenkinsfile}, stage ``Quantize''), running automatically after evaluation. This produces a compressed INT8 model ready for CPU deployment.

All experiments were run on AAU AI-Lab (NVIDIA L4 GPU, AMD EPYC CPU) via Slurm batch jobs.

\section{Deployment}\label{sec:deployment}

This module covers deployment of trained models to edge devices and the importance of endpoint testing and safeguarding.

\subsection{D5.1: Secret Model Deployment on Phone}

The ``secret model'' (ResNet50-v1-12 in ONNX FP32 format) was quantized from FP32 to UInt8 using ONNX Runtime's dynamic quantization:

\begin{verbatim}
from onnxruntime.quantization import (
    QuantType, quantize_dynamic
)
quantize_dynamic(
    'resnet50-v1-12.onnx',
    'resnet50_int8.onnx',
    weight_type=QuantType.QUInt8
)
\end{verbatim}

The quantized model was integrated into the ONNX Runtime Android example app and deployed on a Samsung Galaxy phone. The quantized model replaces the default MobileNet model in the \texttt{readModel()} function of \texttt{MainActivity.kt}.

% SCREENSHOT PLACEHOLDER: Android demo på Samsung Galaxy
% Tag screenshot af: Telefonen der kører ONNX Runtime Android app med ResNet50 (kvantiseret)
% Vis: Klassifikationsresultat + inference tid for FP32 vs UInt8 modellen
\begin{figure}[h]
    \centering
    % \includegraphics[width=0.5\linewidth]{figures/phone_demo_fp32.png}
    % \includegraphics[width=0.5\linewidth]{figures/phone_demo_int8.png}
    \fbox{\parbox{0.9\linewidth}{\centering\vspace{2em}\textbf{PLACEHOLDER:} Screenshot af Samsung Galaxy telefon der kører ONNX Runtime Android app med ResNet50-v1-12. Vis inference tid for FP32 model og UInt8 kvantiseret model side om side\vspace{2em}}}
    \caption{Deployment of the quantized ResNet50 model on a Samsung Galaxy phone using the ONNX Runtime Android app. Left: FP32 original model. Right: UInt8 quantized model with reduced inference time.}
    \label{fig:phone-demo}
\end{figure}

\subsection{D5.2: Endpoint Testing and Safeguarding}

For our Cats vs Dogs classification model deployed as an API endpoint, the following testing dimensions are relevant:

\textbf{Functional testing:}
\begin{itemize}
    \item Verify \texttt{POST /predict} returns correct JSON response with class label and confidence score.
    \item Test with valid cat and dog images to confirm correct classification.
    \item Test with proper error handling for missing or malformed request bodies.
\end{itemize}
This can be automated using \texttt{pytest} with the \texttt{requests} library, or via Postman/Newman for manual and CI/CD-integrated testing.

\textbf{Robustness testing:}
\begin{itemize}
    \item Submit non-image files (PDF, text) and verify graceful error responses.
    \item Submit corrupted image data and verify no server crash.
    \item Submit extremely large images to test memory limits.
    \item Submit images of objects that are neither cats nor dogs (out-of-distribution) to verify the model handles edge cases.
\end{itemize}

\textbf{Performance testing:}
\begin{itemize}
    \item Use Locust to simulate concurrent users and measure response times under load.
    \item Verify latency stays below acceptable thresholds (e.g., $<$200 ms for real-time classification).
    \item Identify the breaking point where throughput degrades.
\end{itemize}

\textbf{Security testing:}
\begin{itemize}
    \item Rate limiting to prevent abuse (e.g., max 100 requests/minute per client).
    \item Input validation to reject payloads exceeding maximum size.
    \item Authentication and API key management.
    \item OWASP ZAP for automated penetration testing (SQL injection, XSS).
\end{itemize}

\textbf{Safeguards against undesired input/output:}
For an image classification model, the primary safeguards are: (1) input validation---rejecting non-image files, oversized images, and rate limiting, (2) confidence thresholds---returning ``unknown'' when the model's softmax probability is below a threshold (e.g., 70\%), which guards against out-of-distribution inputs, and (3) output sanitization---ensuring the API never exposes internal model details or stack traces.

In our specific case, the most important safeguards are confidence thresholds (to handle images that are neither cats nor dogs) and rate limiting (to prevent resource exhaustion on edge devices with limited compute).

\section{Monitoring}\label{sec:monitoring}

This module covers post-deployment monitoring: tracking the carbon footprint of ML training and inference, detecting data and concept drift, and setting up dashboards to visualize pipeline health. The key insight from the course is that ML models degrade silently---unlike traditional software, a deployed model's accuracy is at its best until the moment it enters production, because the world it was trained to predict changes over time.

\subsection{D6.1: Carbon Footprint of Training}

The carbon footprint of training our ResNet18 model was measured using \texttt{carbontracker}, which is integrated directly into the training loop (\texttt{src/train.py}). CarbonTracker monitors GPU power consumption per epoch and estimates CO\textsubscript{2} emissions based on the energy mix of the compute region.

\textbf{Implementation} (from \texttt{src/train.py}):
\begin{verbatim}
tracker = CarbonTracker(
    epochs=config["training"]["epochs"],
    log_dir=str(model_dir),
)
for epoch in range(epochs):
    tracker.epoch_start()
    # ... training + validation ...
    tracker.epoch_end()
tracker.stop()
# log carbon output files as mlflow artifacts
for clog in glob.glob(str(model_dir / "*carbontracker*")):
    mlflow.log_artifact(clog)
\end{verbatim}

\textbf{Measured training footprint} (AAU AI-Lab, NVIDIA L4 GPU, ResNet18, 10 epochs):

\begin{table}[h]
\centering
\caption{Carbon footprint of training (CarbonTracker).}
\label{tab:carbon}
\begin{tabular}{@{} l r @{}}
\toprule
\textbf{Metric} & \textbf{Value} \\
\midrule
Training time per epoch & 32.6 s \\
Total training time (10 epochs) & $\sim$326 s ($\sim$5.4 min) \\
GPU power draw (L4, typical) & $\sim$72 W \\
Total energy (GPU) & $\sim$6.5 Wh \\
CO\textsubscript{2} intensity (Denmark, 2024) & $\sim$100 g CO\textsubscript{2}/kWh \\
\textbf{Estimated CO\textsubscript{2} per training run} & $\sim$\textbf{0.65 g} \\
\bottomrule
\end{tabular}
\end{table}

The carbon footprint of a single training run is negligible ($<$1 g CO\textsubscript{2}) because ResNet18 is a small model (11M parameters) and training converges quickly on the Cats vs Dogs dataset. CarbonTracker results are automatically logged as an MLflow artifact for experiment-level traceability.

\subsection{D6.2: Annual Carbon Footprint Estimate}

To estimate the annual carbon footprint, we use our cost estimator script (\texttt{src/cost\_estimator.py}), which models the full annual cost of operating the ML pipeline based on measured training metrics and deployment assumptions.

\textbf{Assumptions} (from \texttt{results/annual\_cost\_estimate.json}):
\begin{itemize}
    \item \textbf{Retraining}: 52 times per year (weekly) to account for potential drift.
    \item \textbf{Per training}: 0.041 kWh energy, $\sim$1 hour GPU time, 4.39 g CO\textsubscript{2}.
    \item \textbf{Inference}: 1{,}000{,}000 inferences per year at negligible per-inference cost (\$0.000001 each).
    \item \textbf{GPU cost}: \$0.44/hour (cloud equivalent).
    \item \textbf{Energy mix}: 107 g CO\textsubscript{2}/kWh (Denmark regional average).
\end{itemize}

\textbf{Annual estimate:}

\begin{table}[h]
\centering
\caption{Estimated annual carbon footprint and cost for model operation.}
\label{tab:annual-carbon}
\begin{tabular}{@{} l r r r @{}}
\toprule
\textbf{Component} & \textbf{Energy/year} & \textbf{CO\textsubscript{2}/year} & \textbf{Cost/year} \\
\midrule
Weekly retraining ($52 \times 0.041$ kWh) & 2.13 kWh & 228 g (0.23 kg) & \$22.88 \\
Inference (1M inferences) & negligible & negligible & \$1.00 \\
Infrastructure overhead & -- & -- & \$4.58 \\
\midrule
\textbf{Total} & \textbf{$\sim$2.13 kWh} & \textbf{$\sim$0.23 kg CO\textsubscript{2}} & \textbf{\$28.46} \\
\bottomrule
\end{tabular}
\end{table}

The annual carbon footprint is extremely low (0.23 kg CO\textsubscript{2}, equivalent to $\sim$1.9 km of car driving) because ResNet18 is a small model (11M parameters) that trains quickly. The total annual cost of \$28.46 is dominated by GPU compute for weekly retraining (\$22.88), with inference being negligible. For larger models (e.g., LLMs), both training cost and carbon footprint would scale significantly.

\subsection{D6.3: Drift Detection and Mitigation}

\textbf{Types of drift} relevant to our Cats vs Dogs classifier:

\begin{itemize}
    \item \textbf{Data drift}: The distribution of input images changes (e.g., different camera types, lighting conditions, breeds, or image resolutions compared to the training set).
    \item \textbf{Concept drift}: The relationship between features and labels changes (less likely for cat/dog classification, but possible if the definition of ``categories'' shifts or new breeds emerge).
\end{itemize}

\textbf{Detection strategy:}

Our drift detection script (\texttt{src/drift\_detector.py}) detects drift at two levels:

\begin{enumerate}
    \item \textbf{Feature-level drift} (data drift): The Kolmogorov-Smirnov (KS) two-sample test compares the distribution of each feature between a reference dataset and incoming production data. For each feature, the KS statistic and p-value are computed. If the p-value falls below a significance threshold (default: 0.05), drift is flagged for that feature. The script outputs per-feature drift results in a JSON report (\texttt{results/drift\_detection\_report.json}).

    \item \textbf{Performance-level drift} (concept drift): The script compares the model's current accuracy against a baseline accuracy. The accuracy drop is classified into severity levels: \textit{low} ($\leq$5\%), \textit{moderate} ($>$5\%), \textit{high} ($>$10\%), or \textit{critical} ($>$15\%). Based on severity, the script recommends either continued monitoring, close observation, or immediate retraining.
\end{enumerate}

\textbf{Mitigation strategy:}

When drift is detected, the following steps would be taken:
\begin{enumerate}
    \item \textbf{Alert}: Trigger a notification (e.g., via webhook or email) when drift metrics exceed a threshold.
    \item \textbf{Diagnose}: Inspect the drifted samples to understand the nature of the shift (new breeds, lighting changes, out-of-distribution images).
    \item \textbf{Retrain}: Trigger a retraining pipeline (via Jenkins) with updated data that includes representative samples from the shifted distribution.
    \item \textbf{Validate}: Compare the retrained model against the previous version on both old and new data distributions before promoting to production.
    \item \textbf{Deploy}: Use the MLflow Model Registry to promote the new model version to Production stage, which the serving API picks up automatically.
\end{enumerate}

\subsection{D6.4: Monitoring Metrics}

Our pipeline logs the following metrics through MLflow, accessible at \texttt{http://172.24.198.42:5050}:

\begin{itemize}
    \item \textbf{Training metrics}: Per-epoch train/val loss and accuracy, best validation accuracy, training duration
    \item \textbf{Hyperparameters}: Model name, learning rate, batch size, epochs, optimizer, seed, AMP enabled
    \item \textbf{Artifacts}: Best model checkpoint, carbon tracking report, model card
    \item \textbf{Model registry}: Version history with deployment metadata (git commit, deployment timestamp)
    \item \textbf{Serving health}: The \texttt{/health} endpoint reports model load status and device info
\end{itemize}

% SCREENSHOT PLACEHOLDER: MLflow monitoring dashboard
% Tag screenshot fra: http://172.24.198.42:5050
% Vis: En enkelt run-side med Parameters (model_name, lr, batch_size, epochs, optimizer, seed),
%       Metrics grafer (train_loss, val_loss, train_acc, val_acc over epochs),
%       og Artifacts (best_model.pt, carbon_tracking.json, model_card.md)
\begin{figure}[h]
    \centering
    % \includegraphics[width=\linewidth]{figures/mlflow_monitoring.png}
    \fbox{\parbox{0.9\linewidth}{\centering\vspace{2em}\textbf{PLACEHOLDER:} Screenshot af MLflow run-detaljer (172.24.198.42:5050) der viser: (1) Parameters: model\_name, learning\_rate, batch\_size, epochs, optimizer, seed, (2) Metrics-grafer: train\_loss, val\_loss, train\_acc, val\_acc over epochs, (3) Artifacts: best\_model.pt, carbon\_tracking.json, model\_card.md\vspace{2em}}}
    \caption{MLflow run details showing logged parameters, training metric curves, and model artifacts for a completed training run.}
    \label{fig:mlflow-monitoring}
\end{figure}

% SCREENSHOT PLACEHOLDER: MLflow Model Registry
% Tag screenshot fra: http://172.24.198.42:5050/#/models/cats-vs-dogs-model
% Vis: Model Registry med versioner, stage (Production/Staging), og deployment metadata
\begin{figure}[h]
    \centering
    % \includegraphics[width=\linewidth]{figures/mlflow_registry.png}
    \fbox{\parbox{0.9\linewidth}{\centering\vspace{2em}\textbf{PLACEHOLDER:} Screenshot af MLflow Model Registry (172.24.198.42:5050/\#/models) der viser \texttt{cats-vs-dogs-model} med versioner og Production/Staging stage transitions\vspace{2em}}}
    \caption{MLflow Model Registry showing registered model versions with production stage assignments for the \texttt{cats-vs-dogs-model}.}
    \label{fig:mlflow-registry}
\end{figure}

The CarbonTracker output is logged as an MLflow artifact alongside the model checkpoint, providing a complete record of both the model's performance and its environmental cost. This enables tracking how carbon efficiency changes across model versions.

\subsection{Implementation: Exercises}

\textbf{1. CarbonTracker integration} (\texttt{src/train.py}): CarbonTracker wraps the training loop, measuring GPU energy per epoch. Results are saved to JSON and logged as an MLflow artifact.

\textbf{2. Drift detection} (\texttt{src/drift\_detector.py}): Implemented as an automated script that detects both data drift (KS-test on feature distributions) and concept drift (accuracy comparison with severity classification). Results are saved to \texttt{results/drift\_detection\_report.json}.

\textbf{3. MLflow monitoring}: All training runs are tracked in MLflow with full parameter, metric, and artifact logging. The MLflow UI serves as the primary monitoring dashboard for experiment comparison and model version history.

\section{Guest Lecture}\label{sec:guest-lecture}

\section{Post Deployment}\label{sec:post-deployment}

This module covers techniques for managing deployed models over time: continual learning to handle new tasks without forgetting, and machine unlearning to selectively remove learned information. Both exercises use the MNIST digit classification dataset.

\subsection{D7.1: Continual Learning --- Na\"ive vs. Replay + EWC}

Catastrophic forgetting occurs when a neural network trained on new data loses its ability to perform on previously learned tasks. We demonstrate this effect and then mitigate it using experience replay combined with Elastic Weight Consolidation (EWC).

\textbf{Setup:} A simple MLP (784$\to$256$\to$128$\to$10) was trained sequentially on two tasks:
\begin{itemize}
    \item \textbf{Task 1}: Digits 0--4 (5 epochs)
    \item \textbf{Task 2}: Digits 5--9 (5 epochs)
\end{itemize}

\textbf{Na\"ive sequential training} (no replay): After training on Task 2, accuracy on digits 0--4 dropped from 99.2\% to 0.0\%---a complete catastrophic forgetting. The model could only classify new digits (98.4\% on 5--9), giving 47.8\% total accuracy.

\textbf{Experience Replay + EWC}: A memory buffer of 500 samples from Task 1 was stored. During Task 2 training, these were replayed alongside new data. Additionally, EWC applied a regularization penalty ($\lambda=400$) that protects weights important for Task 1, estimated via the diagonal Fisher Information Matrix.

\begin{table}[h]
\centering
\caption{Continual learning results on MNIST (accuracy \%).}
\label{tab:continual}
\begin{tabular}{@{} l r r r @{}}
\toprule
\textbf{Method} & \textbf{Digits 0--4} & \textbf{Digits 5--9} & \textbf{Total} \\
\midrule
After Task 1 only & 99.2 & -- & -- \\
Na\"ive sequential & 0.0 & 98.4 & 47.8 \\
Replay + EWC & 89.2 & 98.5 & 93.7 \\
\bottomrule
\end{tabular}
\end{table}

Replay + EWC recovers 89.2\% accuracy on old digits while maintaining 98.5\% on new digits, demonstrating that the combination of a small memory buffer and weight regularization effectively mitigates catastrophic forgetting. The remaining gap from 99.2\% to 89.2\% on old digits reflects the inherent stability-plasticity tradeoff: some forgetting is unavoidable when adapting to new tasks with limited replay data.

\subsection{D7.2: Machine Unlearning --- Gradient Ascent}

Machine unlearning aims to selectively remove the influence of specific training data from a model without retraining from scratch. This is relevant for GDPR compliance (``right to be forgotten'') and removing biased or copyrighted content.

\textbf{Setup:} A classifier was trained on full MNIST (0--9) achieving 97.5\% baseline accuracy. The goal was to make the model forget digit ``7'' while retaining performance on the remaining digits.

\textbf{Method:} Gradient ascent unlearning maximizes the loss on the forget class, effectively pushing the model away from correctly classifying that digit:

\begin{verbatim}
loss = -criterion(model(x), y)  # maximize loss
loss.backward()
optimizer.step()
\end{verbatim}

After 5 epochs of gradient ascent ($\text{lr}=5 \times 10^{-5}$) on digit 7 samples, followed by 2 epochs of fine-tuning on the remaining classes to stabilize:

\begin{table}[h]
\centering
\caption{Unlearning results: per-class accuracy (\%) before and after forgetting digit 7.}
\label{tab:unlearning}
\begin{tabular}{@{} c r r r @{}}
\toprule
\textbf{Digit} & \textbf{Before} & \textbf{After} & \textbf{Diff} \\
\midrule
0 & 95.9 & 71.4 & $-$24.5 \\
1 & 99.3 & 74.4 & $-$24.8 \\
2 & 97.9 & 77.2 & $-$20.6 \\
3 & 96.3 & 81.8 & $-$14.6 \\
4 & 97.3 & 49.1 & $-$48.2 \\
5 & 95.9 & 68.2 & $-$27.7 \\
6 & 97.7 & 77.7 & $-$20.0 \\
\textbf{7} & \textbf{96.9} & \textbf{0.0} & $-$\textbf{96.9} \\
8 & 99.3 & 49.3 & $-$50.0 \\
9 & 98.1 & 83.3 & $-$14.8 \\
\bottomrule
\end{tabular}
\end{table}

Digit 7 was successfully unlearned (0.0\% accuracy). However, the remaining classes suffered significant collateral damage, dropping from 97.5\% to 63.2\% overall. This illustrates a fundamental challenge in machine unlearning: information is entangled across model parameters, so removing one class affects others. The gradient ascent approach is aggressive by nature---it modifies shared representations, not just class-specific features. More targeted approaches (e.g., fine-grained Fisher-based unlearning or architecture-specific methods) could reduce this collateral impact, but the tradeoff between forgetting completeness and retention quality remains an open research problem.

\subsection{Implementation: Exercises}

\textbf{1. Continual learning} (\texttt{src/continual\_learning.py}): Three-stage experiment on MNIST demonstrating catastrophic forgetting and mitigation via experience replay (500-sample buffer) combined with EWC regularization ($\lambda=400$, diagonal Fisher Information Matrix).

\textbf{2. Machine unlearning} (\texttt{src/unlearning.py}): Gradient ascent unlearning of digit 7 followed by retain fine-tuning. Demonstrates successful targeted forgetting with documented collateral damage to remaining classes.

Both experiments were run on AAU AI-Lab (NVIDIA L4 GPU) via Slurm batch jobs (\texttt{scripts/run\_module7.sh}).

\section{Summary}
\input{summary}

\section{Appendix}\label{sec:appendix}

\subsection{Model Card}
\begin{table}[h]
\centering
\caption{Model Card for Cats vs Dogs Classifier}
\label{tab:modelcard}
\begin{tabularx}{\linewidth}{@{} l X @{}}
\toprule
\textbf{Field} & \textbf{Value} \\
\midrule
\textbf{Model Name} & ResNet18 (Cats vs Dogs) \\
\textbf{Version} & 1.0.0 \\
\textbf{Date} & March 2026 \\
\midrule
\multicolumn{2}{l}{\textbf{Code}} \\
Repository & \url{https://github.com/katrinemie/MLOps-JKP} \\
Commit Hash & \texttt{4855699} \\
Branch & main \\
\midrule
\multicolumn{2}{l}{\textbf{Data}} \\
Dataset & Microsoft Cats vs Dogs (Kaggle) \\
Version & Tracked via DVC (MinIO remote) \\
Storage & MinIO S3: \texttt{s3://pkj-catdog/dvc} \\
Train/Val/Test Split & 70/15/15 \\
\midrule
\multicolumn{2}{l}{\textbf{Training}} \\
Framework & PyTorch 2.x \\
Environment & Docker (\texttt{pytorch/pytorch:2.0.0-cuda11.7-cudnn8-runtime}) / Singularity (AI-Lab) \\
Hardware & NVIDIA L4 24 GB (AI-Lab) / RTX Pro 4000 Ada 20 GB (DAKI cluster) \\
Training Time & $\sim$5.4 min (10 epochs, single GPU) \\
MLflow Run ID & Tracked at \texttt{http://172.24.198.42:5050} \\
\midrule
\multicolumn{2}{l}{\textbf{Hyperparameters}} \\
Learning Rate & 0.001 \\
Batch Size & 32 \\
Epochs & 10 \\
Optimizer & Adam \\
\midrule
\multicolumn{2}{l}{\textbf{Performance}} \\
Accuracy & 96.1\% (val, single GPU) \\
Precision & Logged per run in MLflow \\
Recall & Logged per run in MLflow \\
F1-Score & Logged per run in MLflow \\
\midrule
\multicolumn{2}{l}{\textbf{Limitations \& Risks}} \\
Known Biases & Dataset contains some corrupted images that must be filtered \\
Limitations & Binary classification only (cat/dog), not other animals \\
Intended Use & Educational MLOps pipeline demonstration \\
Out-of-Scope Use & Production pet detection systems without further validation \\
\bottomrule
\end{tabularx}
\end{table}

%%%%%%%%% REFERENCES
{\small
\bibliographystyle{ieee_fullname}
\bibliography{egbib}
}

\end{document}
